/* Flower Animation */
}

.flowers span {
    position: absolute;
    display: block;
    font-size: 24px;
    animation: animateFlowers 15s linear infinite;
    bottom: -50px;
}

/* Different positions and speeds */
.flowers span:nth-child(1) { left: 10%; animation-duration: 12s; }
.flowers span:nth-child(2) { left: 25%; animation-duration: 18s; }
.flowers span:nth-child(3) { left: 40%; animation-duration: 14s; }
.flowers span:nth-child(4) { left: 55%; animation-duration: 20s; }
.flowers span:nth-child(5) { left: 70%; animation-duration: 16s; }
.flowers span:nth-child(6) { left: 85%; animation-duration: 13s; }
.flowers span:nth-child(7) { left: 35%; animation-duration: 17s; }
.flowers span:nth-child(8) { left: 60%; animation-duration: 19s; }

@keyframes animateFlowers {
    0% {
        transform: translateY(0) rotate(0deg);
        opacity: 1;
    }
    100% {
        transform: translateY(-110vh) rotate(360deg);
        opacity: 0;
    }
}
